# pytorch-autotune — local environment template.
# Copy to .env (gitignored) and fill in the keys you have.

# Required for the LLM recommender (Claude provider).
ANTHROPIC_API_KEY=

# Required for the GPT-4o LLM recommender.
OPENAI_API_KEY=

# Optional overrides.
# CLAUDE_MODEL=claude-sonnet-4-6
